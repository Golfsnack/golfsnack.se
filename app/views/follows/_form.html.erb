<div class="follow">
  <% if @user.followed_by?(current_user) %>
    <%= link_to "Sluta följ #{@user.first_name}", unfollow_path(user_id: @user.id), method: :post %>
  <% else %>
    <%= link_to "Följ #{@user.first_name}", follow_path(user_id: @user.id), method: :post %>
  <% end %>
</div>
