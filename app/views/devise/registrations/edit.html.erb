<div class="mt-12">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, multipart: true }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <div class="md:grid md:grid-cols-4 md:gap-6">
      <div class="md:col-span-2">
        <div class="p-8 bg-sand-200">
          <div class="px-4 sm:px-0">
            <h2 class="mb-6 text-xl font-bold">Välkommen upp på första tee!</h2>
          </div>
          <p class="mb-4">Du har hittat hit vilket betyder att du är en av få utvalda som får vara med och testa betaversionen av Golfsnack.se, grattis!</p>
          <h3 class="font-bold text-lg mt-4 mb-4">Vad är Golfsnack?</h3>
          <p class="mb-4">Golfsnack är ett aktivitetsbaserad community. Du får poäng för hur aktiv du är på sidan samt hur aktiv du är som golfspelare. Här kan man dela med sig av sin runda, sin score, dela bilder och mycket mycket mer, det är också här du kommer in i bilden...</p>
          <p class="mb-4">Som vi nämnde tidigare är det en betaversion så vi hoppas att ni har överseende med eventuella buggar och att innehållet är under ständigt arbete.</p>
          <p class="mb-4">Golfsnack ska vara en community “Av golfare, för golfare!“ så därför önskar vi från dig att du klickar dig runt bland funktionerna som finns och sen återkommer med feedback.</p>
          <p class="mb-4">Vi önskar så mycket feedback som möjligt, bra som dåligt, stort som smått, högt & lågt helt enkelt! Det finns en tråd i forumet där du kan lämna din feedback!</p>
          <p class="mb-4">Nu ska vi inte uppehålla dig längre, stort tack för att du tar dig tiden!</p>
          <p class="mb-4">//Golfsnack-redaktionen</p>
        </div>

      </div>
      <div class="mt-5 md:mt-0 md:col-span-2">
        <div class="shadow sm:rounded-md sm:overflow-hidden">
          <div class="px-4 py-5 bg-white sm:p-6">
            <h4 class="mb-6 text-2xl font-bold">Redigera information</h4>
            <div class="grid grid-cols-6 gap-6">
              <div class="col-span-6 sm:col-span-3">
                <%= f.label :first_name %>
                <%= f.text_field :first_name, class: "" %>
              </div>
              <div class="col-span-6 sm:col-span-3">
                <%= f.label :last_name %>
                <%= f.text_field :last_name, class: "" %>
              </div>
              <div class="col-span-6 sm:col-span-3">
                <%= f.label :email %>
                <%= f.email_field :email, autocomplete: "email", class: "" %>
              </div>
              <div class="col-span-6 sm:col-span-3">
                <%= f.label :golf_id %>
                <%= f.text_field :golf_id, class: "" %>
                <span class="hint text-sm text-golfsnack-400">Lämna tomt om du inte vill visa ditt Golf-ID</span>
              </div>
              <div class="block mt-4 col-span-6" data-controller="autocomplete">
                <%= f.label "Hemmaklubb" %>
                <%= f.collection_select :club_id, Club.order(:name).all, :id, :name, {include_blank: true}, { class: "form-input", "data-autocomplete-target": "field" } %>
              </div>
            </div>
            <div class="my-6">
              <%= f.label :info, "Om dig" %>
              <div class="rounded-md shadow-sm">
                <%= f.text_area :info, rows: 5, class: "form-textarea mt-1 block w-full transition duration-150 ease-in-out sm:text-sm sm:leading-5" %>
              </div>
              <p class="mt-2 text-sm text-gray-500">
                Kort beskrivning om vem du är
              </p>
            </div>
          </div>
          <div class="actions">
            <%= f.submit "Uppdatera" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
