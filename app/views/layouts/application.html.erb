<!DOCTYPE html>
<html lang="sv">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Golfsnack.se - Av golfare för golfare</title>
    <link rel="icon" href="/favicon.ico" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' unless Rails.env.development? && Webpacker.config.dev_server[:hmr] %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= render partial: 'og' %>
  </head>
  <body class="bg-fairway-100">
    <%= render partial: 'header' %>
    <div class="container px-12 mx-auto">
      <%= render partial: 'flashes'%>
      <%= yield %>
    </div>
    <%= render partial: "footer" %>
  </body>
</html>
