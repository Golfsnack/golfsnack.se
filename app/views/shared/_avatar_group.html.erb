<div class="flex overflow-hidden">
  <% users.each_with_index do |user, index| %>
    <%= link_to user_path(user) do %>
      <%= image_tag user.avatar.url, class: "inline-block w-8 h-8 #{index > 0 ? "-ml-2" : ""} text-white rounded-full shadow-solid" %>
    <% end %>
  <% end %>
</div>
