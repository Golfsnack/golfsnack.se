<div class="flex flex-row min-w-full py-2 justify-start flex-wrap">
  <% tags.each do |tag| %>
    <a href="/?tag=<%=tag.name%>" class="no-underline rounded bg-golfsnack-100 text-xs mr-2 px-2 py-1" data-turbo-frame="_top">
      #<%= tag.name %>
    </a>
  <% end %>
</div>
