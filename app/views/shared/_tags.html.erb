<div class="flex flex-row min-w-full py-2 justify-start flex-wrap gap-1">
  <% tags.each do |tag| %>
    <a href="/?tag=<%=tag.name%>" class="tag" data-turbo-frame="_top">
      #<%= tag.name %>
    </a>
  <% end %>
</div>
